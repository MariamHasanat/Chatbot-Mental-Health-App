function startConversation() {
    window.location.href = './login.html'; // because we are building the project with the dist folder as the root, all the files will be in the same path as the index.html file
}

export { startConversation };